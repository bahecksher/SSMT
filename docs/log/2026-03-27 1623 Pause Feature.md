# 2026-03-27 1623 Pause Feature

## TL;DR
- What changed: Added a bottom-screen pause button, a paused game state, and a pause menu with `RESUME` and `ABANDON RUN`.
- Why: The user wanted a way to pause mid-run and abandon back to the menu.
- What didn't work: Nothing blocking; build passed. Mobile touch behavior around the bottom button still needs real-device playtesting.
- Next: Playtest pause/resume timing and abandon flow on desktop and mobile.

---

## Full notes

This session intentionally diverged from `docs/plans/2026-03-27 1601 Plan - Responsive Arena Layout.md` because the user explicitly requested a pause feature.

Created `docs/plans/2026-03-27 1620 Plan - Pause Feature.md` for the new work.

Implemented the feature inside the game scene to keep the change targeted:
- added `PAUSED` to the game state model
- added a bottom-centered pause/resume button
- added a modal pause overlay with `RESUME` and `ABANDON RUN`
- added abandon flow back to `MenuScene`
- cleared input anchors on pause/resume to reduce touch-control carryover
- paused scene timers/tweens during pause so delayed calls and UI animations do not keep advancing behind the menu

Verification:
- `npm.cmd run build`
