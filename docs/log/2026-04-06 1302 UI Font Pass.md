# 2026-04-06 1302 UI Font Pass

## TL;DR
- What changed: Added `FreePixel` as a bundled webfont, loaded it during boot, and switched the shared `UI_FONT` constant over to it.
- Why: The user provided a cleaner readable font to pair with the already-selected `pixel_lcd` title font.
- What didn't work: Nothing blocked implementation; title moments intentionally stayed on `pixel_lcd` instead of changing again.
- Next: Playtest the new readable font on mobile-sized layouts and decide whether any text sizes or spacing need a follow-up pass.

---

## Full notes

- Extracted `FreePixel.ttf` from the provided zip into `public/fonts/`.
- Registered `FreePixel` with `@font-face` in `index.html`.
- Updated `UI_FONT` in `src/game/constants.ts` to prefer `FreePixel` with the existing monospace stack as fallback.
- Updated `BootScene` to wait for both `FreePixel` and `pixel_lcd` before starting Menu, reducing visible font swap on first load.
- Verified with `npm.cmd run build`.
