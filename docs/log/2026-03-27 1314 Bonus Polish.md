# 2026-03-27 1314 Bonus Polish

## TL;DR
- What changed: Added collectible bonus-point drops for destroyed enemies and occasional NPC hazard deaths; moved Regent comms into Slick's top slot; reduced both comm window sizes.
- Why: The game needed more rewarding kill feedback and less upper-arena obstruction during dialogue.
- What didn't work: Initial build verification failed inside the sandbox with a Vite `spawn EPERM`; rerunning outside the sandbox succeeded.
- Next: Playtest the bonus pickup values/drop rate and check the shared comm slot on mobile.

---

## Full notes

- Added a new `BonusPickup` entity with drift, short-lived magnet pull, and bright reward styling.
- Enemy ships now always enqueue a 120-point pickup when destroyed inside the arena.
- Hazard-killed NPCs now have a 40% chance to enqueue a 70-point pickup in addition to existing shield-drop behavior.
- `GameScene` now spawns, updates, and collects these pickups and routes collection into unbanked score with a larger floating reward popup.
- Slick and Regent comm panels were both trimmed down to a 70px-high top-center layout so they cover less of the arena.
- Regent now animates into the same top slot as Slick instead of appearing from the bottom.
- Verified with `npm.cmd run build` after rerunning outside the sandbox; build completed successfully with the existing large chunk warning.
