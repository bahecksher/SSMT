# 2026-03-29 0129 Menu Settings Button Spacing

## TL;DR
- What changed: Widened the menu settings panel and increased the spacing between each `ON` and `OFF` button pair.
- Why: The new menu settings buttons were overlapping and needed clearer separation.
- What didn't work: Nothing blocked the fix; it was a small layout adjustment.
- Next: Check the menu on a phone-sized viewport to make sure the wider settings panel still feels balanced.

---

## Full notes

- Increased the settings panel width to give the row controls more horizontal room.
- Repositioned the `ON` and `OFF` buttons from the right edge inward so each pair has a visible gap.
- Verified the project still builds successfully with `npm.cmd run build`.
