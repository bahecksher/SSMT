# 2026-04-03 0030 Git Push

## TL;DR
- What changed: Published the current `main` worktree containing the bomb pickup fix, favor economy retune, and supporting docs updates.
- Why: The user asked to push the latest verified changes to git.
- What didn't work: Nothing blocked the publish step after the local build had already passed.
- Next: Validate the pushed build in-game, especially MissionSelect's `MAX 2` favor state and the bomb pickup wipe path.

---

## Full notes

This was a publish pass rather than a new gameplay feature pass. The worktree already contained:
- the bomb pickup stability fix
- the flatter favor pricing at `2000c`
- the two-favor cap on MissionSelect
- the spec / decision / state / log updates that document those changes

I refreshed `docs/state.md`, added this push log, and prepared the branch for a normal commit-and-push flow on `main`.
