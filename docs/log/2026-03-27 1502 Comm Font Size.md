# 2026-03-27 1502 Comm Font Size

## TL;DR
- What changed: Increased the font size for Slick and Regent comm overlays.
- Why: User wanted the comm lines easier to read and also asked where Regent's lines live for direct editing.
- What didn't work: Nothing blocked; build passed after the UI text-size change.
- Next: Playtest readability on mobile and see whether the larger text needs a panel height adjustment later.

---

## Full notes

- Increased the speaker label font slightly and bumped the main comm body text from 11px to 13px in both comm overlays.
- Regent's editable line bank remains in `src/game/data/regentLines.ts`.
- Verified with `npm.cmd run build`; build succeeded with the same existing large chunk warning.
