{
  "name": "slicks-salvage-mining-training",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs --host 127.0.0.1 --clearScreen false",
    "dev:vite": "vite --configLoader native",
    "build": "tsc && vite build",
    "test": "node --experimental-strip-types test/sharedVersusSimulation.test.ts",
    "partykit:dev": "partykit dev",
    "partykit:deploy": "partykit deploy",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1",
    "partykit": "^0.0.115",
    "typescript": "~5.9.3",
    "vite": "^8.0.1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.100.1",
    "phaser": "^3.90.0"
  }
}
